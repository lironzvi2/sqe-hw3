/* @Provengo summon selenium */

// The Login event defines the actions for logging into the application.
// It fills the username and password text fields and then clicks the login button.
defineEvent(SeleniumSession, "Login", function (session, event) {
  with(session) {

  }
});

defineEvent(SeleniumSession, "ChangeQuizDescription", function (session, event) {
  with(session) {

  }
});

defineEvent(SeleniumSession, "AttemptQuiz", function (session, event) {
  with(session) {

  }
});